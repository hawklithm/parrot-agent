# Keep repository text files stable across Windows, WSL, and Linux checkouts.
* text=auto eol=lf

# Preserve native CRLF for Windows command scripts.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
