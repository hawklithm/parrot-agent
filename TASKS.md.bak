# Paperclip Issues 功能迁移任务

## 执行日期
2026-08-08

## 概述

对比 Paperclip (`~/workspace/paperclip/server/src/routes/issues.ts`) 和 Parrot Agent (`crates/api/src/routes/issues.rs`) 的 Issues 相关功能，发现 **31 个 Paperclip 独有的路由** 需要迁移到 Parrot Agent。

## 路由对比统计

- **Paperclip 总路由数**: 68 个
- **Parrot Agent 当前路由数**: 约 50 个（已实现基础功能）
- **Paperclip 独有路由（缺失）**: 31 个
- **Parrot Agent 独有路由（额外）**: 18 个

## 缺失功能分类

### 1. Comments (评论系统) - 4 个路由 ⭐⭐⭐
**优先级：高** - 核心协作功能

#### 缺失路由
- `GET /issues/:id/comments` - 列出 issue 的所有评论
- `GET /issues/:id/comments/:commentId` - 获取单个评论详情
- `POST /issues/:id/comments` - 创建评论
- `DELETE /issues/:id/comments/:commentId` - 删除评论

#### 相关表
- `issue_comments` (已存在)

#### 迁移范围
- Paperclip: `server/src/routes/issues.ts` (评论相关部分)
- Parrot Agent: 新增到 `crates/api/src/routes/issues.rs`

---

### 2. Attachments (附件系统) - 4 个路由 ⭐⭐⭐
**优先级：高** - 文件上传和管理

#### 缺失路由
- `GET /issues/:id/attachments` - 列出 issue 的所有附件
- `POST /companies/:companyId/issues/:issueId/attachments` - 上传附件
- `GET /attachments/:attachmentId/content` - 下载附件内容
- `DELETE /attachments/:attachmentId` - 删除附件

#### 相关表
- `issue_attachments` (可能需要创建)
- `attachments` (可能需要创建)

#### 技术要点
- 文件上传 (multipart/form-data)
- 文件存储服务集成 (S3 或本地文件系统)
- 内容类型验证
- 文件大小限制

---

### 3. Work Products (工作产物) - 4 个路由 ⭐⭐⭐
**优先级：高** - Agent 执行结果管理

#### 缺失路由
- `GET /issues/:id/work-products` - 列出 issue 的所有工作产物
- `POST /issues/:id/work-products` - 创建工作产物
- `PATCH /work-products/:id` - 更新工作产物
- `DELETE /work-products/:id` - 删除工作产物

#### 相关表
- `issue_work_products` (已存在)

#### 业务逻辑
- 工作产物类型：代码、文档、测试结果等
- 版本管理
- 附件关联

---

### 4. Diagnostics (诊断系统) - 3 个路由 ⭐⭐
**优先级：中** - 调试和问题分析

#### 缺失路由
- `GET /issues/:id/diagnostics/blockers` - Issue 阻塞诊断
- `GET /issues/:id/diagnostics/subtree` - Issue 子树诊断
- `GET /issues/:id/diagnostics/wakes` - Issue 唤醒诊断

#### 技术要点
- 依赖图分析
- 阻塞链检测
- Wakeup 历史追踪
- 性能优化（可能需要缓存）

#### 参考实现
Paperclip: `server/src/services/issues.ts`
- `ISSUE_BLOCKER_DIAGNOSTICS_MAX_BLOCKERS`
- `ISSUE_WAKE_DIAGNOSTICS_LOOKBACK_DAYS`
- `ISSUE_WAKE_DIAGNOSTICS_MAX_ACTIVITY_RECORDS`

---

### 5. Watchdog (监控系统) - 3 个路由 ⭐⭐⭐
**优先级：高** - 自动化任务监控

#### 缺失路由
- `GET /issues/:id/watchdog` - 获取 watchdog 配置
- `PUT /issues/:id/watchdog` - 创建或更新 watchdog
- `DELETE /issues/:id/watchdog` - 删除 watchdog

#### 相关表
- `issue_watchdogs` (可能需要创建)

#### 业务逻辑
- 监控任务状态
- 自动触发恢复动作
- 异常检测和报告

---

### 6. Labels (标签系统) - 3 个路由 ⭐⭐
**优先级：中** - Issue 分类和筛选

#### 缺失路由
- `GET /companies/:companyId/labels` - 列出公司的所有标签
- `POST /companies/:companyId/labels` - 创建标签
- `DELETE /labels/:labelId` - 删除标签

#### 相关表
- `issue_labels` (可能需要创建)
- `labels` (可能需要创建)

#### 业务逻辑
- 标签管理
- Issue-标签关联
- 标签搜索和过滤

---

### 7. Document Locking (文档锁定) - 2 个路由 ⭐
**优先级：低** - 协作编辑冲突预防

#### 缺失路由
- `POST /issues/:id/documents/:key/lock` - 锁定文档
- `POST /issues/:id/documents/:key/unlock` - 解锁文档

#### 技术要点
- 乐观锁或悲观锁实现
- 锁超时机制
- 锁持有者追踪

---

### 8. Low Trust Promotions (低信任输出提升) - 1 个路由 ⭐
**优先级：低** - 安全机制

#### 缺失路由
- `POST /issues/:id/low-trust/promotions` - 提升低信任输出

#### 业务逻辑
- 低信任 Agent 输出审核
- 人工批准流程
- 安全策略管理

---

### 9. Feedback Traces Detail (反馈追踪详情) - 2 个路由 ⭐
**优先级：低** - 调试和分析

#### 缺失路由
- `GET /feedback-traces/:traceId` - 获取反馈追踪详情
- `GET /feedback-traces/:traceId/bundle` - 下载反馈追踪包

---

### 10. External Object Summaries (外部对象摘要批量查询) - 1 个路由 ⭐
**优先级：低**

#### 缺失路由
- `POST /companies/:companyId/issues/external-object-summaries` - 批量查询外部对象摘要

---

### 11. Company Search (公司级搜索) - 1 个路由 ⭐⭐
**优先级：中**

#### 缺失路由
- `GET /companies/:companyId/search` - 跨 Issue 搜索

**注**: Parrot Agent 有 `GET /companies/:companyId/issues/search`，但 Paperclip 的 `/search` 可能是全局搜索，需要确认功能差异。

---

### 12. Document Deletion (文档删除) - 1 个路由 ⭐⭐
**优先级：中**

#### 缺失路由
- `DELETE /issues/:id/documents/:key` - 删除 issue 文档

---

## 🔥 关键业务逻辑缺失

### Issue 创建时自动分配 Agent 并唤醒 ⭐⭐⭐
**优先级：最高** - 调度系统的核心

#### Paperclip 实现 (lines 7054-7062)
```typescript
void queueIssueAssignmentWakeup({
  heartbeat,
  issue,
  reason: "issue_assigned",
  mutation: "create",
  contextSource: "issue.create",
  requestedByActorType: actor.actorType,
  requestedByActorId: actor.actorId,
});
```

#### 问题诊断
- ✅ 数据库有 8 个 issues
- ⚠️ **7/8 issues 的 `assignee_agent_id` 为 NULL**
- ⚠️ **只有 1 个 wakeup request**
- ⚠️ **0 个 routines/triggers**

#### 需要迁移的模块
1. **`issue-assignment-wakeup.ts`** → `crates/services/src/issue_assignment_wakeup.rs`
   - `queueIssueAssignmentWakeup` 函数
   - 集成到 issue 创建、更新流程

2. **Issue 创建流程增强** - `crates/api/src/routes/issues.rs::create_issue`
   - 在创建 issue 后自动调用 wakeup
   - 触发 heartbeat service

3. **Issue 更新流程增强** - `crates/api/src/routes/issues.rs::update_issue`
   - assignee 变更时触发 wakeup
   - status 变更时触发 wakeup

---

## 迁移任务拆解

### Phase 1: 核心调度修复 (P0 - 立即执行)

#### Task 1.1: 实现 Issue Assignment Wakeup 服务
**文件**: `crates/services/src/issue_assignment_wakeup.rs` (新建)

**内容**:
- [x] 从 Paperclip `server/src/services/issue-assignment-wakeup.ts` 迁移 `queueIssueAssignmentWakeup`
- [x] 实现 `IssueAssignmentWakeupService` struct
- [x] 集成 `HeartbeatService` 调用
- [x] 添加错误处理和日志记录
- [x] 单元测试

**依赖**: `HeartbeatService`

---

#### Task 1.2: 修复 Issue 创建流程 - 自动分配 Agent 并唤醒
**文件**: `crates/api/src/routes/issues.rs::create_issue`

**内容**:
- [x] 在 issue 创建成功后调用 `issue_assignment_wakeup::queue_wakeup`
- [x] 确保 `assignee_agent_id` 正确设置
- [x] 添加 activity log: `issue.created`
- [x] 集成测试：验证创建 issue 后自动触发 wakeup

**前置条件**: Task 1.1 完成

---

#### Task 1.3: 修复 Issue 更新流程 - Assignee 变更唤醒
**文件**: `crates/api/src/routes/issues.rs::update_issue`

**内容**:
- [x] 检测 `assignee_agent_id` 变更
- [x] assignee 变更时调用 `queue_wakeup(reason: "issue_reassigned")`
- [x] 添加 activity log: `issue.reassigned`
- [x] 集成测试

**前置条件**: Task 1.1 完成

---

#### Task 1.4: 验证调度系统修复
**工具**: `crates/server/examples/check_scheduling.rs`

**内容**:
- [x] 创建新 issue，验证自动分配 agent
- [x] 验证 wakeup request 自动创建
- [x] 验证 heartbeat run 触发
- [x] 更新 `VERIFICATION_SUMMARY.md`

**前置条件**: Task 1.1-1.3 完成

---

### Phase 2: Comments 评论系统 (P1)

#### Task 2.1: 创建 Comments Service
**文件**: `crates/services/src/issue_comments.rs` (新建)

**内容**:
- [ ] 从 Paperclip 迁移评论相关 service 函数
- [ ] 实现 `IssueCommentsService`
- [ ] CRUD 操作: `create`, `list`, `get`, `delete`
- [ ] 权限检查逻辑
- [ ] 单元测试

---

#### Task 2.2: 实现 Comments API 路由
**文件**: `crates/api/src/routes/issues.rs`

**内容**:
- [ ] `GET /issues/:id/comments` - 列出评论
- [ ] `POST /issues/:id/comments` - 创建评论
- [ ] `GET /issues/:id/comments/:commentId` - 获取评论
- [ ] `DELETE /issues/:id/comments/:commentId` - 删除评论
- [ ] 集成 `IssueCommentsService`
- [ ] 输入验证
- [ ] 集成测试

**前置条件**: Task 2.1 完成

---

### Phase 3: Work Products 工作产物 (P1)

#### Task 3.1: 实现 Work Products Service
**文件**: `crates/services/src/work_products.rs` (可能已存在，需增强)

**内容**:
- [ ] 从 Paperclip `server/src/services/work-products.ts` 迁移
- [ ] CRUD 操作
- [ ] 版本管理
- [ ] 附件关联
- [ ] 单元测试

---

#### Task 3.2: 实现 Work Products API 路由
**文件**: `crates/api/src/routes/issues.rs`

**内容**:
- [ ] `GET /issues/:id/work-products` - 列出工作产物
- [ ] `POST /issues/:id/work-products` - 创建工作产物
- [ ] `PATCH /work-products/:id` - 更新工作产物
- [ ] `DELETE /work-products/:id` - 删除工作产物
- [ ] 集成测试

**前置条件**: Task 3.1 完成

---

### Phase 4: Attachments 附件系统 (P1)

#### Task 4.1: 创建 Attachments 数据库表
**文件**: `migrations/XXX_create_attachments.sql` (新建)

**内容**:
- [ ] 创建 `attachments` 表
- [ ] 创建 `issue_attachments` 关联表
- [ ] 索引优化
- [ ] 运行 migration

---

#### Task 4.2: 实现 Attachments Service
**文件**: `crates/services/src/attachments.rs` (新建)

**内容**:
- [ ] 从 Paperclip 迁移附件上传逻辑
- [ ] 文件存储抽象 (StorageService trait)
- [ ] 本地文件系统实现
- [ ] S3 实现 (可选)
- [ ] 文件类型验证
- [ ] 文件大小限制
- [ ] 单元测试

---

#### Task 4.3: 实现 Attachments API 路由
**文件**: `crates/api/src/routes/issues.rs`

**内容**:
- [ ] `GET /issues/:id/attachments` - 列出附件
- [ ] `POST /companies/:companyId/issues/:issueId/attachments` - 上传附件 (multipart)
- [ ] chments/:attachmentId/content` - 下载附件
- [ ] `DELETE /attachments/:attachmentId` - 删除附件
- [ ] 集成 Axum multipart 处理
- [ ] 流式下载支持
- [ ] 集成测试

**前置条件**: Task 4.1-4.2 完成

---

### Phase 5: Watchdog 监控系统 (P1)

#### Task 5.1: 创建 Watchdog 数据库表
**文件**: `migrations/XXX_create_watchdogs.sql` (新建)

**内容**:
- [ ] 创建 `issue_watchdogs` 表
- [ ] 字段: agent_id, schedule, next_check_at, status 等
- [ ] 索引优化
- [ ] 运行 migration

---

#### Task 5.2: 实现 Watchdog Service
**文件**: `crates/services/src/issue_watchdogs.rs` (新建)

**内容**:
- [ ] 从 Paperclip `server/src/services/task-watchdogs.ts` 迁移
- [ ] CRUD 操作
- [ ] 调度逻辑
- [ ] 异常检测
- [ ] 单元测试

---

#### Task 5.3: 实现 Watchdog API 路由
**文件**: `crates/api/src/routes/issues.rs`

**内容**:
- [ ] `GET /issues/:id/watchdog` - 获取 watchdog
- [ ] `PUT /issues/:id/watchdog` - 创建/更新 watchdog
- [ ] `DELETE /issues/:id/watchdog` - 删除 watchdog
- [ ] 集成测试

**前置条件**: Task 5.1-5.2 完成

---

### Phase 6: Labels 标签系统 (P2)

#### Task 6.1: 创建 Labels 数据库表
**文件**: `migrations/XXX_create_labels.sql` (新建)

**内容**:
- [ ] 创建 `labels` 表
- [ ] 创建 `issue_labels` 关联表
- [ ] 索引优化
- [ ] 运行 migration

---

#### Task 6.2: 实现 Labels Service
**文件**: `crates/services/src/labels.rs` (新建)

**内容**:
- [ ] CRUD 操作
- [ ] Issue-Label 关联管理
- [ ] 单元测试

---ask 6.3: 实现 Labels API 路由
**文件**: `crates/api/src/routes/issues.rs` 或新建 `crates/api/src/routes/labels.rs`

**内容**:
- [ ] `GET /companies/:companyId/labels` - 列出标签
- [ ] `POST /companies/:companyId/labels` - 创建标签
- [ ] `DELETE /labels/:labelId` - 删除标签
- [ ] 集成测试

**前置条件**: Task 6.1-6.2 完成

---

### Phase 7: Diagnostics 诊断系统 (P2)

#### Task 7.1: 实现 Diagnostics Service
**文件**: `crates/services/src/issue_diagnostics.rs` (新建)

**内容**:
- [ ] 从 Paperclip `server/src/services/issues.ts` 迁移诊断函数
- [ ] `diagnose_blockers` - 阻塞链分析
- [ ] `diagnose_subtree` - 子树状态分析
- [ ] `diagnose_wakes` - Wakeup 历史分析
- [ ] 性能优化（图遍历、缓存）
- [ ] 单元测试

---

#### Task 7.2: 实现 Diagnostics API 路由
**文件**: `crates/api/src/routes/issues.rs`

**内容**:
- [ ] `GET /issues/:id/diagnostics/blockers`
- [ ] `GET /issues/:id/diagnostics/subtree`
- [ ] `GET /issues/:id/diagnostics/wakes`
- [ ] 集成测试

**前置条件**: Task 7.1 完成

---

### Phase 8: 其他功能 (P3 - 低优先级)

#### Task 8.1: Document Locking
- [ ] 实现文档锁定机制
- [ ] `POST /issues/:id/documents/:key/lock`
- [ ] `POST /issues/:id/documents/:key/unlock`

---

#### Task 8.2: Low Trust Promotions
- [ ] 实现低信任输出审核
- [ ] `POST /issues/:id/low-trust/promotions`

---

#### Task 8.3: Feedback Traces Detail
- [ ] `GET /feedback-traces/:traceId`
- [ ] `GET /feedback-traces/:traceId/bundle`

---

#### Task 8.4: External Object Summaries Batch
- [ ] `POST /companies/:companyId/issues/external-object-summaries`

---

#### Task 8.5: Document Deletion
- [ ] `DELETE /issues/:id/documents/:key`

---

#### Task 8.6: Company Search
- [ ] 确认与现有 `/companies/:companyId/issues/search` 的差异
- [ ] 如有必要，实现全局搜索

---

## 优先级总结

### 🔴 P0 - 立即执行 (本周)
1. **Issue Assignment Wakeup** (Task 1.1-1.4)
   - 修复调度系统核心问题
   - 验证 issue 创建后自动分配 agent 并唤醒

### 🟠 P1 - 高优先级 (2-4 周)
2. **Comments** (Task 2.1-2.2)
3. **Work Products** (Task 3.1-3.2)
4. **Attachments** (Task 4.1-4.3)
5. **Watchdog** (Task 5.1-5.3)

### 🟡 P2 - 中优先级 (1-2 个月)
6. **Labels** (Task 6.1-6.3)
7. **Diagnostics** (Task 7.1-7.2)

### 🟢 P3 - 低优先级 (按需)
8. **其他功能** (Task 8.1-8.6)

---

## 依赖关系图

```
Phase 1 (P0) ──┬─→ Phase 2 (Comments)
               ├─→ Phase 3 (Work Products)
               ├─→ Phase 4 (Attachments)
               └─→ Phase 5 (Watchdog)

Phase 2-5 ────→ Phase 6 (Labels)
              └─→ Phase 7 (Diagnostics)

所有 Phase ───→ Phase 8 (其他功能)
```

---

## 验证标准

每个 Phase 完成后需要：
1. ✅ 所有单元测试通过
2. ✅ 集成测试覆盖核心场景
3. ✅ API 文档更新
4. ✅ 运行 `check_scheduling` 工具验证
5. ✅ 更新 `VERIFICATION_SUMMARY.md`

---

## 估算工作量

- **Phase 1 (P0)**: 3-5 天
- **Phase 2-5 (P1)**: 2-3 周
- **Phase 6-7 (P2)**: 1-2 周
- **Phase 8 (P3)**: 按需，约 1 周

**总计**: 约 6-8 周完整迁移

---

## 相关文件

### Paperclip 参考
- `~/workspace/paperclip/server/src/routes/issues.ts`
- `~/workspace/paperclip/server/src/services/issue-assignment-wakeup.ts`
- `~/workspace/paperclip/server/src/services/issues.ts`
- `~/workspace/paperclip/server/src/services/work-products.ts`
- `~/workspace/paperclip/server/src/services/task-watchdogs.ts`

### Parrot Agent 实现
- `crates/api/src/routes/issues.rs`
- `crates/services/src/` (各 service 模块)
- `crates/models/src/` (数据模型)

### 工具和文档
- `crates/server/examples/check_scheduling.rs` - 调度诊断工具
- `VERIFICATION_SUMMARY.md` - 验证总结文档
